{{> header}}
<div class="profile col-md-6 text-center col-lg">
  <img class="profile-img" src="./images/user.png" />
  <div class="profile-info">
    <p>Username: <small>{{username}}</small></p>
    {{#if count}}
    <p class="infoType">Wished {{count}} treks =)</p>

    {{#each treks}}
    <p>{{this.name}}</p>
    {{/each}}

    {{else}}
    <p>No treks</p>
    {{/if}}
  </div>
</div>
{{> footer}}